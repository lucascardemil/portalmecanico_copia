
load data local infile '/var/www/supra/vehicle_brand_models.csv' \
	into table vehicle_brand_models fields \
		terminated by ';' \
		enclosed by '"' \
		escaped by '' \
	ignore 1 lines (id, brand, model);

load data local infile '/var/www/supra/vehicle_years.csv' \
	into table vehicle_years fields \
		terminated by ';' \
		enclosed by '"' \
		escaped by '' \
	ignore 1 lines (id, v_id, v_year);

load data local infile '/var/www/supra/vehicle_engines.csv' \
	into table vehicle_engines fields \
		terminated by ';' \
		enclosed by '"' \
		escaped by '' \
	ignore 1 lines (id, year_id, v_engine);

